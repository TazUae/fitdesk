/**
 * Dashboard-level constants.
 *
 * ERP-derived values are computed in app/dashboard/page.tsx and passed as
 * props — those don't belong here. This file is for static UX targets.
 */

/** Weekly session-completion goal shown on the dashboard quick-actions row. */
export const WEEKLY_SESSION_GOAL = 20
